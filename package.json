{
  "name": "staticwebapp-plugeventstools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"No build step for this static site\" && exit 0",
    "build:azure": "npm run build"
  }
}
